import { reasons } from "@/data/schoolData";
import { Section, SectionHeader } from "./Shared";
export function WhyChooseUs() { return <Section><SectionHeader eyebrow="Why choose us" title="A foundation for life, not only the classroom" text="An approach designed to nurture capable, thoughtful, and confident young people." /><div className="mt-14 grid gap-px overflow-hidden border border-border bg-border sm:grid-cols-2 lg:grid-cols-3">{reasons.map(({ title, text, icon: Icon }, i) => <article key={title} className="group bg-background p-7 transition-colors hover:bg-secondary sm:p-8"><div className="flex items-start justify-between"><span className="grid size-11 place-items-center bg-primary text-gold"><Icon className="size-5" /></span><span className="font-display text-sm text-muted-foreground/50">0{i + 1}</span></div><h3 className="mt-7 font-display text-2xl font-semibold text-primary">{title}</h3><p className="mt-3 leading-7 text-muted-foreground">{text}</p></article>)}</div></Section>; }
