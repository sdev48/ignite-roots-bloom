import { Mail, MapPin, Phone, Map } from "lucide-react";
import { school } from "@/data/schoolData";
import { Section, SectionHeader } from "./Shared";
const details = [{ label: "Address", value: school.address, icon: MapPin }, { label: "Phone", value: school.phone, icon: Phone }, { label: "Email", value: school.email, icon: Mail }];
export function Contact() { return <Section id="contact" className="bg-secondary"><div className="grid gap-12 lg:grid-cols-[0.8fr_1.2fr] lg:gap-16"><div><SectionHeader eyebrow="Contact us" title="We would be pleased to hear from you" align="left" /><div className="mt-8"><h3 className="font-display text-2xl font-semibold text-primary">{school.name}</h3><p className="mt-2 text-muted-foreground">{school.location}</p></div><address className="mt-8 space-y-5 not-italic">{details.map(({ label, value, icon: Icon }) => <div key={label} className="flex gap-4"><span className="grid size-10 shrink-0 place-items-center bg-primary text-gold"><Icon className="size-4" /></span><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-muted-foreground">{label}</p><p className="mt-1 font-semibold text-foreground">{value}</p></div></div>)}</address><p className="mt-8 border-l-2 border-gold pl-4 text-sm leading-6 text-muted-foreground">Official contact and admission information will be added once supplied by the school.</p></div><div className="grid min-h-[420px] place-items-center border border-border bg-background text-center shadow-sm"><div className="max-w-xs px-6"><Map className="mx-auto size-11 text-gold-dark" /><h3 className="mt-5 font-display text-2xl font-semibold text-primary">Google Maps</h3><p className="mt-3 text-sm leading-6 text-muted-foreground">[Official map location will appear here once the school address or coordinates are confirmed.]</p></div></div></div></Section>; }
