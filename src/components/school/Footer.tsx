import { Facebook, Instagram, Youtube } from "lucide-react";
import { Button } from "@/components/ui/button";
import { navItems, school } from "@/data/schoolData";
import { Brand } from "./Shared";
export function Footer() { return <footer className="bg-primary text-primary-foreground"><div className="mx-auto max-w-7xl px-5 py-16 sm:px-8 lg:px-10"><div className="grid gap-12 border-b border-primary-foreground/15 pb-12 lg:grid-cols-[1.2fr_1fr_0.7fr]"><div><Brand inverse /><p className="mt-5 max-w-sm text-sm leading-6 text-primary-foreground/60">A nurturing learning environment in {school.location}.</p></div><div><h2 className="text-xs font-bold uppercase tracking-[0.2em] text-gold">Quick links</h2><div className="mt-5 grid grid-cols-2 gap-x-6 gap-y-3">{navItems.map(([label, id]) => <a key={id} href={`#${id}`} className="text-sm text-primary-foreground/65 transition-colors hover:text-gold">{label}</a>)}</div></div><div><h2 className="text-xs font-bold uppercase tracking-[0.2em] text-gold">Follow us</h2><div className="mt-5 flex gap-2">{[{ name: "Facebook", icon: Facebook }, { name: "Instagram", icon: Instagram }, { name: "YouTube", icon: Youtube }].map(({ name, icon: Icon }) => <Button key={name} variant="social" size="icon" disabled title={`${name} link to be added`} aria-label={`${name} link unavailable`}><Icon /></Button>)}</div><p className="mt-4 text-xs text-primary-foreground/45">Official social links coming soon.</p></div></div><p className="pt-7 text-center text-xs text-primary-foreground/50">© 2026 Singapore International School. All Rights Reserved.</p></div></footer>; }
