import { facilities } from "@/data/schoolData";
import { Section, SectionHeader } from "./Shared";
export function Facilities() { return <Section id="facilities"><SectionHeader eyebrow="Spaces for growth" title="Facilities" text="The areas below are editable placeholders. Official information and school photographs can be added once confirmed." /><div className="mt-12 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{facilities.map(({ title, image, icon: Icon }, index) => <article key={title} className={`group relative overflow-hidden bg-primary ${index === 0 || index === 5 ? "sm:col-span-2" : ""}`}><img src={image} alt={`Placeholder image for ${title}`} width={1200} height={912} loading="lazy" className="h-64 w-full object-cover opacity-75 transition duration-500 group-hover:scale-105 group-hover:opacity-60" /><div className="absolute inset-0 bg-image-overlay" /><div className="absolute inset-x-0 bottom-0 p-5"><Icon className="mb-3 size-5 text-gold" /><h3 className="font-display text-xl font-semibold text-primary-foreground">{title}</h3><p className="mt-1 text-xs text-primary-foreground/65">[Confirm official availability]</p></div></article>)}</div></Section>; }
