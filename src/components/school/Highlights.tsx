import { highlights } from "@/data/schoolData";
export function Highlights() { return <section aria-label="School highlights" className="bg-gold py-12 text-primary"><div className="mx-auto grid max-w-7xl grid-cols-2 gap-y-10 px-5 sm:px-8 lg:grid-cols-4 lg:px-10">{highlights.map(({ value, label }) => <div key={label} className="border-primary/20 px-4 text-center lg:border-l first:lg:border-l-0"><strong className="block font-display text-4xl font-semibold sm:text-5xl">{value}</strong><span className="mt-2 block text-xs font-bold uppercase tracking-[0.18em]">{label}</span></div>)}</div></section>; }
