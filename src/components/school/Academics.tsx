import { ArrowUpRight, CheckCircle2 } from "lucide-react";
import { academicStages } from "@/data/schoolData";
import { Section, SectionHeader } from "./Shared";
const approach = ["Student-focused learning", "Strong academic foundations", "Practical understanding", "Critical thinking", "Creativity", "Communication & collaboration"];
export function Academics() { return <Section id="academics" className="bg-secondary"><div className="grid gap-12 lg:grid-cols-[0.8fr_1.2fr] lg:gap-20"><div><SectionHeader eyebrow="Our academic approach" title="Learning That Inspires" align="left" text="We aim to make learning purposeful, engaging, and connected to the world students experience." /><div className="mt-8 grid gap-3 sm:grid-cols-2 lg:grid-cols-1">{approach.map(item => <div key={item} className="flex items-center gap-3 text-sm font-semibold text-foreground"><CheckCircle2 className="size-5 text-gold-dark" />{item}</div>)}</div></div><div className="grid gap-4">{academicStages.map(({ title, text, icon: Icon }, i) => <article key={title} className="group flex items-center gap-5 border border-border bg-background p-6 shadow-sm transition-transform hover:-translate-y-0.5 sm:p-8"><span className="grid size-14 shrink-0 place-items-center bg-primary text-gold"><Icon className="size-6" /></span><div className="min-w-0 flex-1"><p className="text-xs font-bold uppercase tracking-[0.18em] text-gold-dark">Stage 0{i + 1}</p><h3 className="mt-2 font-display text-2xl font-semibold text-primary">{title}</h3><p className="mt-2 text-sm text-muted-foreground">{text}</p></div><ArrowUpRight className="hidden size-5 text-muted-foreground sm:block" /></article>)}</div></div></Section>; }
